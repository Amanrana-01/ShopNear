/** Seed geography anchor (spec §11) — Navrangpura, Ahmedabad. Used as the
 * default map-pin centre in the registration wizard and as the search
 * anchor when building the aggregated product pool (see api/catalogue.ts). */
export const NEIGHBOURHOOD_ANCHOR = { lat: 23.0365, lng: 72.5611 }
