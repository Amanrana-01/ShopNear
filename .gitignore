# Dependencies
node_modules/

# Build output
dist/
build/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Archived pre-rebuild prototype (Express+MySQL / Next.js / Expo).
# Kept on disk for reference only; the committed history of its backend
# lives on the `legacy/nextjs-express-prototype` branch. Its web/ and
# mobile/ source was never committed upstream, so these files are the
# only copy — do not delete this directory.
legacy/

# Uploads (mock verification docs, shop photos) — regenerated by seed
apps/api/uploads/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Cached Open Food Facts API responses. Regenerated by the image pipeline and
# only there to make re-runs free — not source, and not review material.
apps/web-customer/.cache/
